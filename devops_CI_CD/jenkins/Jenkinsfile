pipeline {
    agent any

    options {
        timestamps()
        skipDefaultCheckout(true)
    }

    environment {
        AWS_REGION    = 'us-east-1'
        SCRIPTS_PATH  = 'devops_CI_CD/scripts/ephemeral_agent'
        ANSIBLE_PATH  = 'devops_CI_CD/ansible'
    }

    stages {
        stage('Sparse Checkout') {
            steps {
                checkout([
                    $class: 'GitSCM',
                    userRemoteConfigs: scm.userRemoteConfigs,
                    branches: scm.branches,
                    extensions: [
                        [$class: 'SparseCheckoutPaths', sparseCheckoutPaths: [
                            [$class: 'SparseCheckoutPath', path: "${env.SCRIPTS_PATH}"],
                            [$class: 'SparseCheckoutPath', path: "${env.ANSIBLE_PATH}"]
                        ]]
                    ]
                ])
            }
        }

        stage('Bootstrap EC2 Agent') {
            steps {
                dir(env.SCRIPTS_PATH) {
                    sh 'chmod +x 0[1-7]-*.sh'
                    sh './01-fetch-master.sh'
                    sh './02-sg-iam.sh'
                    sh './03-launch-agent.sh'
                    sh './04-docker-run.sh'
                    sh './05-health-check.sh'
                    sh './06-version-check.sh'
                    sh './07-export-env.sh'
                }
            }
        }

        stage('Load Ephemeral Agent Env') {
            steps {
                dir(env.SCRIPTS_PATH) {
                    script {
                        def props = readProperties file: 'ephem_env.txt'
                        props.each { k,v -> env[k] = v }
                        echo "Agent IP: ${env.AGENT_IP}"
                    }
                }
            }
        }

        stage('Run Ansible Playbook via SSM') {
            steps {
                withCredentials([[$class: 'AmazonWebServicesCredentialsBinding', credentialsId: 'aws-credentials']]) {
                    dir(env.ANSIBLE_PATH) {
                        sh '''
                            echo "Running Ansible against ${AGENT_IP}..."
                            source ../.venv/bin/activate
                            ANSIBLE_HOST_KEY_CHECKING=False \
                              ansible-playbook -i inventory/hosts playbooks/deploy/playbook.yml \
                              -e "ansible_host=${AGENT_IP}"
                        '''
                    }
                }
            }
        }
    }

    post {
        success { echo 'Pipeline completed successfully.' }
        failure { echo 'Pipeline failed.' }
    }
}
