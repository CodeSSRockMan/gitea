worker_processes  1;

events {
    worker_connections  1024;
}

http {
    upstream backend {
        server {{PRIMARY}} max_fails=3 fail_timeout=5s;
        server {{BACKUP}} backup;
    }

    server {
        listen 8080;

        location / {
            proxy_pass http://backend;
        }
    }
}
